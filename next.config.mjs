/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '',
    assetPrefix: '',
    images: {
      loader: 'default',
      unoptimized: true,
    },
  }

export default nextConfig;
